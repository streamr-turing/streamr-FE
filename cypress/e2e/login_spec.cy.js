{/* <reference types="Cypress" />

describe('Testing Login Page', () => {

    beforeEach(() => {
        cy.intercept('POST',https://streamr-be.herokuapp.com/graphql).as('request')
        cy.visit('http://localhost:3000/login')
        cy.wait('@request')
      })

    it('True should be true', () => {
        expect('true').to.equal('true')
    })
}) */}